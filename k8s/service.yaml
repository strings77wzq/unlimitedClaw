apiVersion: v1
kind: Service
metadata:
  name: golem-gateway
  namespace: golem
spec:
  type: ClusterIP
  selector:
    app: golem
    component: gateway
  ports:
    - port: 80
      targetPort: 18790
      protocol: TCP

apiVersion: v1
kind: Secret
metadata:
  name: {{ include "golem.fullname" . }}-secrets
  labels:
    {{- include "golem.labels" . | nindent 4 }}
type: Opaque
data:
  openai-api-key: {{ .Values.secrets.openai_api_key | b64enc | quote }}
